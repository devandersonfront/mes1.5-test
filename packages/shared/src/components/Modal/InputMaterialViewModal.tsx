import React from 'react'
import {BaseModal} from "./BaseModal";

export const InputMaterialViewModal = ({isVisible}) => {
    return (
        <BaseModal
            isOpen={isVisible}
            style={{}}
        >
                <div>안녕</div>
        </BaseModal>
    )
}

